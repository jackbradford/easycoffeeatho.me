/**
 * This file provides the Store page, which serves as the "Directory" for the
 * online store. It's a portal to every item on the site by category. Each
 * category also has its own page.
 *
 *
 */
import React, { Component } from 'react';

export default class Store extends Component {

    render() {

        return (
            <React.Fragment>
            <main className="home">
                <p>Homepage</p>
            </main>
            </React.Fragment>
        );
    }
}

